/* let lenghtNumber = parseInt(prompt('Введите длину массива'));

let result = []
for ( let i = 0; i < lenghtNumber; i++) {

    let arrayValues = parseInt(prompt('Введите значения массива'));
    result.push(arrayValues);
    alert(result);
    
    minIndex = result.indexOf(Math.min(...result)),
    maxIndex = result.indexOf(Math.max(...result));

}
[result[minIndex], result[maxIndex]] = [result[maxIndex], result[minIndex]];
alert('Мінімальний =' + Math.min(...result));
alert('Максимальний =' + Math.max(...result));
alert('Новий масив =' + result); */
